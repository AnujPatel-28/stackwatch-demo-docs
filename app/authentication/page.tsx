import {DocsShell,PageIntro,Code} from "../components/DocsShell";
export default function Authentication(){return <DocsShell current="/authentication"><PageIntro eyebrow="GUIDE · API V3" title="Authentication" description="Authenticate every ForgeKit API request with a workspace API key sent in the Authorization header."/><section className="doc-section" id="overview"><h2>API keys</h2><p>ForgeKit uses bearer tokens. Create and revoke keys from Workspace settings. Keys are shown only once, so store them in a secrets manager.</p><Code>Authorization: Bearer fk_live_your_key</Code><h3>Required scopes</h3><p>Project creation requires <code>projects:write</code>; reading project details requires <code>projects:read</code>.</p></section><section className="doc-section"><h2>Errors</h2><p>A missing key returns <code>401 unauthorized</code>. A valid key without the required scope returns <code>403 forbidden</code>.</p></section></DocsShell>}
