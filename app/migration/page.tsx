import {DocsShell,PageIntro,Code} from "../components/DocsShell";
export default function Migration(){return <DocsShell current="/migration"><PageIntro eyebrow="MIGRATION GUIDE · V2 → V3" title="Migrate to API v3" description="Move an existing ForgeKit API v2 integration to the stable v3 contract in three small steps."/><div className="content-block" id="overview"><h2>What changed</h2><p>API v3 uses bearer authentication, standard JSON errors, and the <code>framework</code> project field. Existing project IDs remain valid.</p><div className="mt-5 rounded-xl border-l-4 border-amber-400 bg-amber-50 p-4 text-sm text-amber-900"><strong>Deprecation notice:</strong> API v2 is deprecated and will be removed on 2027-02-01.</div></div><div className="content-block"><h2>1. Update the base URL</h2><Code>https://api.forgekit.dev/api/projects</Code><h2>2. Update authentication</h2><p>Replace the legacy <code>X-API-Key</code> header with the bearer token format.</p><Code>Authorization: Bearer fk_live_your_key</Code><h2>3. Test and roll out</h2><p>Send a test project request, confirm the 201 response, then deploy. No data migration is required.</p></div><div className="content-block"><h2>Compatibility notes</h2><p>V3 accepts the v2 <code>name</code> value without changes. V3 does not accept form-encoded bodies; send JSON instead.</p></div></DocsShell>}
