import {DocsShell,PageIntro,Code} from "../components/DocsShell";
export default function Webhooks(){return <DocsShell current="/webhooks"><PageIntro eyebrow="GUIDE · EVENTS" title="Webhooks" description="Receive near real-time notifications when a project changes state or a technology signal is detected."/><div className="doc-panel" id="overview"><h2>Configure an endpoint</h2><p>Add an HTTPS endpoint in Workspace settings. ForgeKit sends a signed JSON payload with each event and expects a 2xx response within five seconds.</p><h3>Event types</h3><p><code>project.created</code> fires when a project is registered. <code>technology.updated</code> fires when a tracked dependency changes.</p></div><div className="doc-panel"><h2>Payload</h2><Code>{'{ "id": "evt_01HZXC2Q", "type": "project.created", "data": { "project_id": "proj_01HZX8M4R7" } }'}</Code></div><div className="doc-panel"><h2>Retries and verification</h2><p>Failed deliveries are retried three times with exponential backoff. Verify the <code>X-ForgeKit-Signature</code> header before processing.</p></div></DocsShell>}
